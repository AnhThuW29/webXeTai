// IP MAY
const ip = "localhost";

const portServer = "8000";

const URL_IMAGES = "http://" + ip + ":" + portServer + "/public/images/";
const URL_REQUESTAXIOS = "http://" + ip + ":" + portServer;

export { URL_IMAGES, URL_REQUESTAXIOS };
